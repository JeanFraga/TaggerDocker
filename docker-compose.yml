version: '3.7'
services:
    flask:
        build: ./flask
        container_name: flask
        restart: always
        expose:
            - 8080
        volumes:
            - web-data:/home/ubuntu/app/

volumes:
    web-data: