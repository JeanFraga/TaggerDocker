version: '3.7'
services:
    flask:
        build: ./flask
        container_name: flask
        restart: always
        ports:
            - 80:80
        volumes:
            - web-data:/home/ubuntu/app/

volumes:
    web-data: